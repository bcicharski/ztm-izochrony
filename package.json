{
  "name": "ztm-izochrony",
  "version": "1.0.0",
  "private": true,
  "description": "Dokąd dojadę? — izochrony komunikacji miejskiej (strona statyczna + narzędzia prekompilacji GTFS)",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "lint": "eslint js tools tests",
    "check": "npm run lint && npm test",
    "serve": "node tools/serve.mjs"
  },
  "devDependencies": {
    "eslint": "^9.14.0",
    "globals": "^15.12.0",
    "gtfs-realtime-bindings": "^2.2.0"
  }
}
